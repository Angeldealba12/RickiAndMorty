const LocationInfo = ( {name, type, dimension} ) => {
    return (
        <div>
            <p>Name: {name}</p>
            <p>type: {type}</p>
            <p>dimension: {dimension}</p>
        </div>
    )
} 

export default LocationInfo;

